<div class="mx-auto md:w-2/3 w-full pt-16">
  <h1 class="font-bold text-4xl">Editing mailbox</h1>

  <%= render 'form', mailbox: @mailbox %>

  <%= link_to 'Show this mailbox', @mailbox, class: 'ml-2 rounded-lg py-3 px-5 bg-gray-100 inline-block font-medium' %>
  <%= link_to 'Back to mailboxes', mailboxes_path, class: 'ml-2 rounded-lg py-3 px-5 bg-gray-100 inline-block font-medium' %>
</div>
